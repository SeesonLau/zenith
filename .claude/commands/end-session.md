Do all of the following without asking for confirmation:

1. Read `_project-docs/memory-bank/activeContext.md`, `_project-docs/memory-bank/progress.md`, `_project-docs/roadmap.md`, and `CHANGELOG.md`.
2. Rewrite `_project-docs/memory-bank/activeContext.md` — update current state checklist, open issues, and next steps based on what was done this session. Keep it under 50 lines.
3. If the version changed this session: update `_project-docs/memory-bank/progress.md` and append a new entry to `CHANGELOG.md`.
4. If any roadmap items were completed or new ones identified: update `_project-docs/roadmap.md` (move completed items to the Completed section, add new items to Planned).
5. If `_project-docs/progress/qa-bugs.md` exists, check if any issues fixed this session match open rows in it. For each one fixed, update its Status to `✅ Fixed YYYY-MM-DD`. Update the Tracking table counts at the bottom.
6. Write a new session log at `_project-docs/sessions/YYYY-MM-DD-vX.X.X.md` (use today's date and current version). Include: what was done, files created/modified/deleted, any pending manual steps, known issues, next session priorities.

After all files are written, output a one-paragraph summary of what changed and what's next.
